{
  "indexes": [
    {
      "//": "Search by name: listed clinicians only, ordered for a prefix range. The listed filter is what the rules require, so it is part of the query rather than applied afterwards.",
      "collectionGroup": "clinicianDirectory",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "listed", "order": "ASCENDING" },
        { "fieldPath": "searchName", "order": "ASCENDING" }
      ]
    },
    {
      "//": "How often something planned has already been finished, which decides whether to ask how it went. Counted rather than fetched, and bounded to days before today so the answer does not depend on whether the tick that triggered it has synced yet.",
      "collectionGroup": "completions",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "activityId", "order": "ASCENDING" },
        { "fieldPath": "date", "order": "ASCENDING" }
      ]
    }
  ],
  "fieldOverrides": [
    {
      "collectionGroup": "circle",
      "fieldPath": "memberUid",
      "indexes": [
        {
          "order": "ASCENDING",
          "queryScope": "COLLECTION"
        },
        {
          "order": "ASCENDING",
          "queryScope": "COLLECTION_GROUP"
        }
      ]
    }
  ]
}
